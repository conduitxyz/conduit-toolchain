{
  "name": "conduit-toolchain",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "main": "dist/index.js",
  "keywords": ["conduit", "conduit.xyz", "ethereum"],
   "scripts": {
    "build": "ncc build src/index.js -o dist --source-map"
  },
  "dependencies": {
    "@actions/core": "^1.8.0",
    "@actions/tool-cache": "^1.7.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.33.1"
  }
}
